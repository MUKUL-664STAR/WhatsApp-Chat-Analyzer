{
  "watch": ["src", ".env"],
  "ext": "js,ts,json",
  "ignore": ["src/**/*.spec.ts", "src/**/*.test.ts", "build/**/*"],
  "exec": "ts-node --transpile-only ./src/app.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
